<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FileSystemViewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileSystemViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:mControls="using:ModernControls.Controls"
    Title="FileSystem-Viewer">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="TitleBar"
            Grid.Row="0" 
            Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
            Title="File Viewer"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False">

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/appIcon.png"/>
            </TitleBar.IconSource>

            <TitleBar.LeftHeader>
                <Button
                    Command="{x:Bind MainPageViewModel.OpenSettingsMenuCommand}"
                    Margin="5,0,-5,0"
                    Background="Transparent"
                    BorderThickness="0.5"
                    Width="35"
                    Height="35">

                    <Viewbox
                        Margin="-1.2">
                        <FontIcon
                            Glyph="&#xE713;"/>
                    </Viewbox>
                </Button>
            </TitleBar.LeftHeader>
        </TitleBar>

        <Grid
            Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="0.4*"/>
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="0.2*"/>
                </Grid.RowDefinitions>
                
                <ScrollViewer
                    Grid.Row="0"
                    HorizontalScrollBarVisibility="Visible" 
                    VerticalScrollBarVisibility="Disabled"
                    HorizontalScrollMode="Enabled"
                    VerticalScrollMode="Disabled">
                    <Frame 
                        x:Name="RootFrame"/>
                </ScrollViewer>

                <controls:GridSplitter
                    
                    Grid.Row="1"/>

                <Grid
                    Grid.Row="2">

                    <mControls:HierarchicalTreemap
                        x:Name="hierarchicalTreemap"
                        GlyphIcon="&#xE838;"
                        Separator="\"
                        ItemsSource="{x:Bind MainPageViewModel.TreemapNodes, Mode=OneWay}"/>

                    <Button
                        Style="{StaticResource CommandBarButtonStyle}"
                        ToolTipService.ToolTip="Open in new window"
                        Content="&#xE8A7;"
                        Height="27"
                        Width="35"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,5,5,0"
                        Command="{x:Bind MainPageViewModel.OpenTreeMapNewWindowCommand}">
                    </Button>
                </Grid>
            </Grid>

            <controls:GridSplitter
                Grid.Column="1"/>

            <Frame
                x:Name="ChartFrame"
                Grid.Column="2"/>

            <Frame
                x:Name="SettingsFrame"
                Visibility="{x:Bind MainPageViewModel.ApplicationState.SettingsMenuVisibility, Mode=OneWay}"
                Grid.Column="0"
                Grid.ColumnSpan="3"/>
        </Grid>
    </Grid>
</Window>
