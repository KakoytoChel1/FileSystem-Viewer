<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FileSystemViewer.Views.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileSystemViewer.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FileSystemViewer.ViewModels"
    mc:Ignorable="d"
    xmlns:models="using:FileSystemViewer.Models"
    xmlns:view="using:FileSystemViewer.Views"
    xmlns:converters="using:FileSystemViewer.Views.Converters"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:mControls="using:ModernControls"
    x:Name="mainPage">

    <Page.Resources>
        <converters:BytesIntoSuitableFormatConverter x:Key="BytesIntoSuitableFormatConverter"/>
        <converters:DoubleIntoStringConverter x:Key="DoubleIntoStringConverter"/>
        <converters:EnumPropertyToIntConverter x:Key="EnumPropertyToIntConverter"/>
        <converters:ColorToSolidColorBrushConverter x:Key="ColorToSolidColorBrushConverter"/>

        <DataTemplate x:Key="FileSystemNodeTemplate">
            <TreeViewItem ItemsSource="{Binding FileSystemNodes, Mode=OneWay}" IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="Name" Width="500"/>
                        <ColumnDefinition x:Name="ProgressBar" Width="100"/>
                        <ColumnDefinition x:Name="Percentage" Width="auto" MinWidth="50"/>
                        <ColumnDefinition x:Name="Size" Width="100"/>
                        <ColumnDefinition x:Name="FileCount" Width="100"/>
                        <ColumnDefinition x:Name="LastModifiedDate" Width="auto" MinWidth="170"/>
                        <ColumnDefinition Width="0.1*"/>
                    </Grid.ColumnDefinitions>

                    <!--General information-->
                    <StackPanel
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left"
                        Spacing="10"
                        Grid.Column="0"
                        Width="450"
                        ToolTipService.ToolTip="{Binding Name}">

                        <!--Icon-->
                        <FontIcon 
                            Glyph="{Binding UnicodeIcon}"
                            Foreground="{Binding IconColor, Converter={StaticResource ColorToSolidColorBrushConverter}}"
                            FontSize="16"
                            FontFamily="Segoe Fluent Icons"
                            VerticalAlignment="Center"/>

                        <!--Name-->
                        <TextBlock 
                            Text="{Binding Name}"
                            TextTrimming="CharacterEllipsis"/>

                        <TextBlock
                            Text="{Binding Tag}"
                            FontSize="13"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <Border Background="Transparent" Grid.Column="1" Margin="2,0,2,0">
                        <Grid>
                            <ProgressBar 
                                Value="{Binding PercentProperty, Mode=OneWay}"/>
                        </Grid>

                        <i:Interaction.Behaviors>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="True">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Collapsed"/>
                            </i:DataTriggerBehavior>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="False">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Visible"/>
                            </i:DataTriggerBehavior>
                        </i:Interaction.Behaviors>
                    </Border>

                    <Border Background="Transparent" Grid.Column="2" Margin="2,0,2,0">
                        <Grid>
                            <TextBlock 
                                Text="{Binding PercentProperty, Converter={StaticResource DoubleIntoStringConverter}, Mode=OneWay}"
                                HorizontalAlignment="Right"
                                Style="{StaticResource DirectoryNodeInfoTextBlockStyle}"/>
                        </Grid>

                        <i:Interaction.Behaviors>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="True">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Collapsed"/>
                            </i:DataTriggerBehavior>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="False">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Visible"/>
                            </i:DataTriggerBehavior>
                        </i:Interaction.Behaviors>
                    </Border>

                    <!--Size-->
                    <Border Background="Transparent" Grid.Column="3" Margin="2,0,2,0"   >
                        <StackPanel 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="5">

                            <TextBlock
                                Text="{Binding Size, Converter={StaticResource BytesIntoSuitableFormatConverter}, Mode=OneWay}"
                                Style="{StaticResource DirectoryNodeInfoTextBlockStyle}"/>

                            <TextBlock
                                Text="&#xE9F9;"
                                VerticalAlignment="Center"
                                Style="{StaticResource DirectoryNodeInfoTextIconStyle}"/>

                        </StackPanel>

                        <i:Interaction.Behaviors>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="True">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Collapsed"/>
                            </i:DataTriggerBehavior>
                            <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="False">
                                <i:ChangePropertyAction PropertyName="Visibility" Value="Visible"/>
                            </i:DataTriggerBehavior>
                        </i:Interaction.Behaviors>
                    </Border>

                    <!--File count-->
                    <Border Background="Transparent" Grid.Column="4" Margin="2,0,2,0">
                        <StackPanel 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="5">

                            <TextBlock
                                Text="{Binding FileCount, Mode=OneWay}"
                                Style="{StaticResource DirectoryNodeInfoTextBlockStyle}"/>

                            <TextBlock
                                Text="&#xE8A5;"
                                VerticalAlignment="Center"
                                Style="{StaticResource DirectoryNodeInfoTextIconStyle}"/>
                        </StackPanel>

                    <i:Interaction.Behaviors>
                        <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="True">
                            <i:ChangePropertyAction PropertyName="Visibility" Value="Collapsed"/>
                        </i:DataTriggerBehavior>
                        <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="False">
                            <i:ChangePropertyAction PropertyName="Visibility" Value="Visible"/>
                        </i:DataTriggerBehavior>
                    </i:Interaction.Behaviors>
                </Border>

                <Border
                    Margin="10,0,0,0"
                    Grid.Column="1"
                    Grid.ColumnSpan="2">

                    <StackPanel
                        Orientation="Horizontal"
                        Spacing="10">

                        <ProgressRing
                            IsActive="True"
                            Width="15"
                            Height="15"/>

                        <TextBlock
                            FontSize="13"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="In progress..."/>
                    </StackPanel>

                    <i:Interaction.Behaviors>
                        <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="False">
                            <i:ChangePropertyAction PropertyName="Visibility" Value="Collapsed"/>
                        </i:DataTriggerBehavior>
                        <i:DataTriggerBehavior Binding="{Binding IsInProgress}" ComparisonCondition="Equal" Value="True">
                            <i:ChangePropertyAction PropertyName="Visibility" Value="Visible"/>
                        </i:DataTriggerBehavior>
                        </i:Interaction.Behaviors>
                    </Border>

                    <!--Last modification date-->
                    <Border Background="Transparent" Grid.Column="5" Margin="2,0,2,0">
                        <StackPanel 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="5">

                            <TextBlock
                                Margin="10,0,0,0"
                                Text="{Binding LastModified}"
                                Style="{StaticResource DirectoryNodeInfoTextBlockStyle}"/>

                            <TextBlock
                                Text="&#xF738;"
                                VerticalAlignment="Center"
                                Style="{StaticResource DirectoryNodeInfoTextIconStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TreeViewItem>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--Buttons bar-->
        <StackPanel
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Margin="5,5,5,0"
            Orientation="Horizontal"
            Spacing="3">

            <Button
                Style="{StaticResource CommandBarButtonStyle}"
                ToolTipService.ToolTip="Select target..."
                Command="{x:Bind ViewModel.OpenTargetSelectDialogCommand}"
                CommandParameter="{Binding ElementName=mainPage, Path=XamlRoot}"
                Content="&#xE958;"
                Foreground="{ThemeResource AccentAAFillColorDefaultBrush}">
            </Button>

            <!--Scanning managing command bar-->
            <Border
                BorderThickness="0.5,0,0.5,0"
                BorderBrush="{ThemeResource ControlElevationBorderBrush}"
                Padding="5,0,5,0">

                <StackPanel Orientation="Horizontal" Spacing="3">
                    <Button
                        Style="{StaticResource MiniCommandBarButtonStyle}"
                        ToolTipService.ToolTip="Stop"
                        Command="{x:Bind ViewModel.CancelScanningCommand}"
                        CommandParameter="{Binding ElementName=mainPage, Path=XamlRoot}"
                        Content="&#xEE95;"
                        Foreground="{ThemeResource SystemFillColorCriticalBrush}">
                    </Button>

                    <Button
                        Style="{StaticResource MiniCommandBarButtonStyle}"
                        ToolTipService.ToolTip="Resume"
                        Command="{x:Bind ViewModel.ResumeScanningCommand}"
                        Content="&#xF5B0;"
                        Foreground="{ThemeResource SystemFillColorSuccessBrush}">
                    </Button>

                    <Button
                        Style="{StaticResource MiniCommandBarButtonStyle}"
                        ToolTipService.ToolTip="Pause"
                        Command="{x:Bind ViewModel.PauseScanningCommand}"
                        Content="&#xF8AE;"
                        Foreground="{ThemeResource SystemFillColorCautionBrush}">
                    </Button>

                </StackPanel>
            </Border>

            <Button
                Style="{StaticResource CommandBarButtonStyle}"
                Content="&#xE895;"
                FontSize="16"
                ToolTipService.ToolTip="Refresh scanning"
                Command="{x:Bind ViewModel.RefreshScanningCommand}"
                CommandParameter="{Binding ElementName=mainPage, Path=XamlRoot}">
            </Button>

            <Button
                Style="{StaticResource CommandBarButtonStyle}"
                Content="&#xE8F7;"
                FontSize="16"
                ToolTipService.ToolTip="Refresh selected"
                Command="{x:Bind ViewModel.RescanSelectedDirectoriesCommand}"
                CommandParameter="{Binding ElementName=mainPage, Path=XamlRoot}">
            </Button>

            <Border
                BorderThickness="0.5,0,0,0"
                BorderBrush="{ThemeResource ControlElevationBorderBrush}">
                
                <Button
                    Margin="5,0,0,0"
                    Style="{StaticResource CommandBarButtonStyle}"
                    ToolTipService.ToolTip="Open in new window"
                    Content="&#xE8A7;">
                </Button>
            </Border>
        </StackPanel>

        <TreeView 
            Grid.Row="1" 
            x:Name="FileSystemTreeView"
            ItemsSource="{x:Bind ViewModel.DriveNodes, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedFileSystemNode, Mode=TwoWay}"
            ItemTemplate="{StaticResource FileSystemNodeTemplate}"
            ScrollViewer.VerticalScrollBarVisibility="Visible"/>

        <ProgressBar 
            Grid.Row="2" 
            Background="Transparent"
            VerticalAlignment="Top"
            BorderThickness="0">

            <i:Interaction.Behaviors>
                <!--InProgress-->
                <i:DataTriggerBehavior Binding="{x:Bind ViewModel.ApplicationState.CurrentScanningState, Mode=OneWay, Converter={StaticResource EnumPropertyToIntConverter}}" ComparisonCondition="Equal" Value="4">
                    <i:ChangePropertyAction PropertyName="Foreground" Value="{ThemeResource SystemFillColorAttentionBrush}"/>
                    <i:ChangePropertyAction PropertyName="IsIndeterminate" Value="True"/>
                </i:DataTriggerBehavior>

                <!--Paused-->
                <i:DataTriggerBehavior Binding="{x:Bind ViewModel.ApplicationState.CurrentScanningState, Mode=OneWay, Converter={StaticResource EnumPropertyToIntConverter}}" ComparisonCondition="Equal" Value="2">
                    <i:ChangePropertyAction PropertyName="Foreground" Value="{ThemeResource SystemFillColorCautionBrush}"/>
                    <i:ChangePropertyAction PropertyName="Value" Value="100"/>
                    <i:ChangePropertyAction PropertyName="IsIndeterminate" Value="False"/>
                </i:DataTriggerBehavior>

                <!--Canceled-->
                <i:DataTriggerBehavior Binding="{x:Bind ViewModel.ApplicationState.CurrentScanningState, Mode=OneWay, Converter={StaticResource EnumPropertyToIntConverter}}" ComparisonCondition="Equal" Value="3">
                    <i:ChangePropertyAction PropertyName="Foreground" Value="{ThemeResource SystemFillColorCriticalBrush}"/>
                    <i:ChangePropertyAction PropertyName="Value" Value="100"/>
                    <i:ChangePropertyAction PropertyName="IsIndeterminate" Value="False"/>
                </i:DataTriggerBehavior>

                <!--Completed-->
                <i:DataTriggerBehavior Binding="{x:Bind ViewModel.ApplicationState.CurrentScanningState, Mode=OneWay, Converter={StaticResource EnumPropertyToIntConverter}}" ComparisonCondition="Equal" Value="1">
                    <i:ChangePropertyAction PropertyName="Foreground" Value="{ThemeResource SystemFillColorSuccessBrush}"/>
                    <i:ChangePropertyAction PropertyName="IsIndeterminate" Value="False"/>
                    <i:ChangePropertyAction PropertyName="Value" Value="100"/>
                </i:DataTriggerBehavior>
            </i:Interaction.Behaviors>
        </ProgressBar>
    </Grid>
</Page>
