<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FileSystemViewer.Views.Windows.TreemapWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileSystemViewer.Views.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FileSystemViewer.ViewModels"
    mc:Ignorable="d"
    xmlns:models="using:FileSystemViewer.Models"
    xmlns:view="using:FileSystemViewer.Views"
    xmlns:converters="using:FileSystemViewer.Views.Converters"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:mControls="using:ModernControls.Controls"
    Title="TreemapWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="TitleBar"
            Grid.Row="0" 
            Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
            Title="Treemap window"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False">

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/drive.ico"/>
            </TitleBar.IconSource>
        </TitleBar>

        <mControls:HierarchicalTreemap
            x:Name="hierarchicalTreemap"
            Grid.Row="1"
            GlyphIcon="&#xE838;"
            Separator="\"
            ItemsSource="{x:Bind ViewModel.TreemapNodes, Mode=OneWay}"/>
    </Grid>
</Window>
